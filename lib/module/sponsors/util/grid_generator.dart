import 'package:flutter/material.dart';

class GridGenerator {
  static Widget generateGrid(List images) {
    return Container();
  }
}

class GridBigLeft extends StatelessWidget {
  const GridBigLeft({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class GridBigRight extends StatelessWidget {
  const GridBigRight({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class GridThreeSmall extends StatelessWidget {
  const GridThreeSmall({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
